"""Model definition, training and inference.

Training routines take a :class:`~packagename.config.Settings` and an optional
:class:`~packagename.tracking.ExperimentTracker`, so they are callable both from
the ``packagename-train`` entrypoint and from a notebook.
"""
